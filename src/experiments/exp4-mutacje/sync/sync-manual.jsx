import { Link } from "react-router-dom";

export default function ManualMutationSync() {
  return (
    <div className="exp4">
      <h2 className="exp4__title">
        PB4 — 4c Сравнение стратегий синхронизации после мутации
      </h2>

      <p>
        Данный раздел представляет сравнительный анализ результатов,
        полученных в подэкспериментах 4a и 4b. В них были рассмотрены две
        мутации server-state: добавление нового элемента и удаление
        существующего элемента.
      </p>

      <p>
        В обоих случаях сравнивались две стратегии синхронизации интерфейса
        после успешной мутации: повторное получение данных через API
        (refetch) и ручное изменение локального состояния приложения
        (setState).
      </p>

      <p>
        Стратегия refetch приводит к дополнительному обращению к API после
        мутации. Благодаря этому интерфейс формируется на основании повторно
        полученного состояния серверных данных, а разработчику не требуется
        вручную воспроизводить изменение коллекции на клиенте.
      </p>

      <p>
        При использовании setState повторное получение списка не выполняется.
        После успешной мутации локальное состояние изменяется непосредственно
        в компоненте. Это позволяет сократить количество обращений к API,
        однако требует от разработчика самостоятельно обеспечить соответствие
        локального состояния результату серверной операции.
      </p>

      <p>
        Результаты добавления и удаления показывают одну и ту же общую
        закономерность: при ручном управлении server-state синхронизация после
        мутации не определяется отдельным механизмом управления данными.
        Разработчик самостоятельно выбирает стратегию и реализует необходимую
        логику обновления состояния.
      </p>

      <p>
        Отдельный эксперимент для 4c не проводится, поскольку этот пункт не
        вводит нового типа мутации или новой исследуемой переменной. Его
        назначение — сопоставить результаты уже выполненных сценариев 4a и 4b.
        Создание ещё одного интерактивного сценария фактически повторило бы
        исследованную логику и не предоставило бы новых данных для сравнения.
      </p>

      <p>
        Таким образом, 4c является аналитическим итогом PB4, основанным на
        экспериментальных наблюдениях из разделов 4a и 4b.
      </p>

      <div>
        <p>
          <Link to="/exp4/add">
            ← Вернуться к 4a — Добавление элемента
          </Link>
        </p>

        <p>
          <Link to="/exp4/delete">
            ← Вернуться к 4b — Удаление элемента
          </Link>
        </p>

        <p>
          <Link to="/exp4">← Назад к эксперименту 4</Link>
        </p>
      </div>
    </div>
  );
}